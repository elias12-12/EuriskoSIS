# frontend

React student portal and admin panel. Scaffolded in Phase 6 (see
[PROJECT_PLAN.md](../PROJECT_PLAN.md)); a `frontend` service joins
`docker-compose.yml` at that point.

Directory exists from Phase 0 so the repo layout is fixed from day one.
