# Normalise to LF in the repo. Without this, files authored on Windows arrive in
# the Linux backend container with CRLF endings, which breaks any shell script or
# entrypoint we add later with an opaque "no such file or directory".
* text=auto eol=lf

# Binary sources: never touch these.
*.pdf  binary
*.xlsx binary
